node_modules
.next
out
.env
.env.local
.env*.local
# Env backups/copies also hold real secrets — never commit them (e.g. .env.local.save, .env.local.bak)
.env*.local*
.env.local.*
*.log
.DS_Store
.vercel
next-env.d.ts
*.tsbuildinfo

# iOS — never commit a real API key
ios/**/Secrets.plist
**/xcuserdata/
*.xcuserstate

# Supabase CLI local link/state — internal, never commit
supabase/.temp/

# Atelier is a separate product with its own git repo — keep it out of Forge's tree
atelier/
